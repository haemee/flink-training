CREATE TABLE filtered_trx (
                                    id varchar(20) NOT NULL,
                                    krw_amount BIGINT NOT NULL,
                                    ts TIMESTAMP(3) NOT NULL
);
